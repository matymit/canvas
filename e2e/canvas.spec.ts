/**
 * Archived legacy Playwright smoke test.
 * See `test-results/archive/playwright/canvas-legacy.spec.ts` for the preserved implementation.
 *
 * This placeholder prevents the old spec from being picked up accidentally while documenting
 * where the legacy test now lives.
 */
