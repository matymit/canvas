// src/test/setupCanvas.ts
// Lightweight Canvas API harness for Konva in Vitest (jsdom)
// Option A: vitest-canvas-mock
import 'vitest-canvas-mock';

// If deeper APIs are needed later (text metrics), consider node-canvas or
// exposing measurement overrides on a case-by-case basis.
